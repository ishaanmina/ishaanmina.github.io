{% comment %}
  "Ask My AI-twin" chat widget. Included once from _layouts/default.liquid.
  The backend URL comes from _config.yml (chatbot_url) so it is not hardcoded
  in the JavaScript; the JS reads it from the data-endpoint attribute below.
  If chatbot_url is empty the whole widget is skipped.
{% endcomment %}
{% if site.chatbot_url %}
  <div id="chat-widget" data-endpoint="{{ site.chatbot_url }}" data-email="{{ site.data.socials.email }}">
    <div id="chat-callout" class="chat-callout" role="status" hidden>
      <strong>Ask anything about my work.</strong> An AI answers as me &mdash; it's a bot, not a live chat.
      <button id="chat-callout-close" type="button" aria-label="Dismiss">&times;</button>
    </div>

    <button id="chat-toggle" type="button" aria-label="Ask an AI twin of Ishaan" aria-expanded="false" aria-controls="chat-panel">
      <i class="fa-solid fa-robot" aria-hidden="true"></i>
      <span class="chat-toggle-label">Ask My AI-twin</span>
    </button>

    <section id="chat-panel" role="dialog" aria-labelledby="chat-title" hidden>
      <header class="chat-header">
        <div>
          <div id="chat-title" class="chat-title"><i class="fa-solid fa-robot" aria-hidden="true"></i> My AI-twin</div>
          <div class="chat-disclaimer">A bot answering as me &mdash; not me, and it may get things wrong.</div>
        </div>
        <button id="chat-close" type="button" aria-label="Close chat">&times;</button>
      </header>

      <div id="chat-messages" class="chat-messages" aria-live="polite"></div>

      <div id="chat-starters" class="chat-starters">
        <button type="button" class="chat-starter">What did you do at imec?</button>
        <button type="button" class="chat-starter">What roles are you looking for?</button>
        <button type="button" class="chat-starter">Tell me about the chip you taped out</button>
        <button type="button" class="chat-starter">Can you work in Belgium without sponsorship?</button>
      </div>

      <form id="chat-form" class="chat-form" autocomplete="off">
        <input id="chat-input" type="text" placeholder="Ask about my work…" maxlength="5000" required aria-label="Your question">
        <button id="chat-send" type="submit" aria-label="Send">
          <i class="fa-solid fa-paper-plane" aria-hidden="true"></i>
        </button>
      </form>
    </section>
  </div>
{% endif %}
